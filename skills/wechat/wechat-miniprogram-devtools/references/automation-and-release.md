# Automation And Release

Load only the topic reference needed for the current task:

- `automation-pipeline.md`: stable DevTools startup, automator sessions, stage timeouts, and logs.
- `cache-and-devtools-state.md`: generated output freshness, compile cache, DevTools state, and command-help discipline.
- `web-view-real-device.md`: remote H5, `web-view`, maps, location, keyboard, and real-device pitfalls.
- `screenshots.md`: simulator screenshot reliability and OS/window capture fallback.
- `preview-upload.md`: preview QR, upload/release safety, artifact validation, and common release mistakes.

The installed DevTools CLI and project-local `miniprogram-automator` version are the command/API authority. Re-check `official-sources.md` and local help output before using exact flags.
